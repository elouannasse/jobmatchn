{
  "name": "jobmatchn",
  "private": true,
  "description": "Plateforme de mise en relation de talents et d'entreprises",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspaces --if-present",
    "build": "npm run build --workspaces --if-present",
    "test": "npm run test --workspaces --if-present"
  }
}
